# .envrc — Automatically activates local venv and shows (venv) in prompt

export VIRTUAL_ENV="$(realpath ./venv)"
export PATH="$VIRTUAL_ENV/bin:$PATH"
export VIRTUAL_ENV_PROMPT="(venv) "

# Optional: export any project-specific env vars
# export OPENAI_API_KEY=sk-xxxx...
